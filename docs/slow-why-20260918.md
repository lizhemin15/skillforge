# 这轮到底慢在哪（真浏览器帧时序账本）

日期：2026-09-18　环境：线上 `http://127.0.0.1:8092`（v2609182128）
尺子：`scripts/measure_chat_latency.py`（DUMP 时间线，只打变化点）、`web/tests/chat_material_e2e.py`
原则：每段静默都是**量出来的秒数**，不是「感觉慢」。

---

## 一、writing（技能写稿）整轮 59.6s

| 时刻 | 步骤 | 材料 | 正文 | 说明 |
|---|---|---|---|---|
| 0.2s | ① 意图分析 | 0 字 | 0 | 起始 |
| 4.6s | ① 意图分析 | 5 字 | 0 | ← **前 4.4s 屏幕零变化** |
| 6.2s | ① 意图分析 | 54 字 | 0 | 材料在滚 |
| 9.7s | ④ 执行中 | 26 字 | 0 | ← **前 3.5s 零变化**（阶段切换） |
| 12.0s | ④ 执行中 | 163 字 | 0 | 材料顶到截尾上限（160 字） |
| 51.8s | ④ 执行中 | 163 字 | 15 字 | ← **前 39.8s 零变化**（真凶之一） |
| 59.6s | — | 0 字 | 819 字 | 正文顺滑流完（~0.2s / 25 字） |

材料窗口统计：275 帧 / 只有 108 个不同尾部快照 / 峰值 163 字。
**12.0s 之后尾部快照一个都没变** —— 起草这一跳的 content 流（460 片）压根没接进材料槽。

## 二、news2word 复刻，同 session 两轮：第 1 轮 **647.3s**、第 2 轮 31.6s

### 第 1 轮（慢的那轮）
```
[   0.0s] ① 意图分析 active
[   2.5s] 首片中间材料（reasoning 流出来的）
[   6.0s] 材料滚到 ~160 字上限附近 …………………………… 然后停
[ 616.5s] ① 意图分析｜识别出的意图与 action：write/write   ← 610.5s 后突然活了
[ 641.0s] 首正文
[ 647.3s] 结束
```

### 第 2 轮（同 session「整理成 Word」）
```
[   1.7s] 首片中间材料
[  21.6s] 意图 docgen/gen
[  31.6s] 交付物《星禾科技数据中台3.0发布新闻稿.docx》
```
→ **news2word 链路本身不慢（31.6s）**，慢是同一个 stall 的**间歇性**发作。

---

## 三、根因：两条，都在流式层

### R1（主凶）流式客户端没有「读空闲看门狗」
- `internal/llm/stream.go:289`：`streamHTTPClient()` 超时 = **10 分钟**。
- 分类跳在 **6.0s 就把答案流完了**，之后 provider 既不关连接、也不给字节，我们干等到
  600s 客户端超时 → 实测 **616.5s**（= 600s 超时 + 16.5s 重试）才继续。
- **用户说的「一直卡着计时」就是这 610.5 秒。** 不是模型慢，是我们没有「多久没字节就掀桌」的尺子。

### R2 流被截断时不校验「有没有正常结束」
- 半截正文直接丢给 `json.Unmarshal` → 服务端日志：
  ```
  [classify-retry] unmarshal err=unexpected end of JSON input raw_out="{\""
  [classify-retry] unmarshal err=unexpected end of JSON input raw_out="{"
  ```
  （20:38、20:39、20:44 共三条，raw_out 只有 2~3 字节）
- 于是白跑一次 classify-retry：**这一跳的账直接翻倍**。

### F1（次要但显眼）起草跳的材料在 12.0s 断粮
- `chat.go` 只挂了 4 处 `clock.Thinking`（分类跳 / 未命中兜底 / docgen 分支），**起草、审稿、改稿一条都没挂**。
- `internal/agent/progress.go` 里 `contentSink` 就是为这种「关了思考链但 content 在流」的跳准备的，现成没用上。
- 结果：39.8 秒里模型正在逐字吐正文，屏幕上却只有那个跳动的计时器。

---

## 四、修法（按收益排序）

1. **加读空闲看门狗**（stream.go）：无字节超过 ~20s 即 abort，按 transient 重试。
   610s 的卡顿直接压到 20s 量级。同时给分类跳设 hop 级预算（它是「按规则选格子」的活，25s 足够）。
2. **流结束必须校验终止信号**（`[DONE]` / `finish_reason`）：不完整当错误，绝不拿去解析。
   消掉 `raw_out="{\""` 这类白跑的重试。
3. **起草/改稿跳接 `contentSink`**：让 39.8s 那段有字在动（尾部 160 字一路滚），
   这是用户点名要的「中间可以流式输出思考的一些中间材料」。
4. （可选）材料顶到上限后改为滚动更新而非冻结，避免「满 160 字后就一动不动」的观感。

---

## 五、修前 / 修后对照（待补）

| 场景 | 修前 | 修后 |
|---|---|---|
| 起草跳最长静默 | **39.8s** | 待测 |
| 分类跳最长静默 | **610.5s** | 待测 |
| 整轮（写稿） | 59.6s / 647.3s（间歇炸） | 待测 |
| 整轮（news2word） | 31.6s（正常时） | 待测 |
| 分类跳截断重试 | 有（raw_out=`{"`） | 待测 |
